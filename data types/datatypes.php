<?php

include '../variables/variabelen.php';

$leeg = null;
echo gettype($leeg);
echo empty($leeg);

//echo gettype($zon);

// TODO:
    // Vraag het type op voor de die staan in het bestand variabelen.php
    // Wat is het type voor $waarde, bekijk de pagina: https://www.php.net/manual/en/language.types.float.php